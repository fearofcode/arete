# !/bin/bash

read -r -p "This will destroy the local database. Are you sure? [y/N] " response
case $response in
    [yY][eE][sS]|[yY])
        dropdb arete && createdb arete && psql -d arete -f ~/Dropbox/arete/arete_backup.sql
        ;;
    *)
        echo "Exiting."
        ;;
esac